@model IEnumerable<ThAmCo.Events.Data.Staff>

@{
    ViewData["Title"] = "Index";
}

<h1>Staff List</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("First name")
            </th>
            <th>
                @Html.DisplayName("Last name")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StaffId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StaffId">Details</a> |
                <a asp-controller="EventGuest" asp-action="StaffIndex" 
                   asp-route-id="@item.StaffId">Event List</a>
            </td>
        </tr>
}
    </tbody>
</table>
